# Backend API base URL (no trailing slash)
# Local development — point at the FastAPI server:
REACT_APP_BACKEND_URL=http://localhost:8000

# On Vercel production, leave this blank (empty string) so requests use relative /api paths.

# Optional: enable webpack health-check endpoints in dev
# ENABLE_HEALTH_CHECK=true
